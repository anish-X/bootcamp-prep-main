// YOUR CODE BELOW

const myReverse = (array) => {
    debugger;
    let reversedValue = [];
    for(let i = array.length -1; i >= 0; i--){
        reversedValue.push(array[i]);
    }
    return reversedValue;
    // let reversedArray = array.reverse();
    // return reversedArray;
}
